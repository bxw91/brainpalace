"""brainpalace-life — the life-memory product subpackage.

Empty scaffold (Phase 8). Product code (P1+) lands here and may import the
engine ONLY through the seam allowlist enforced by
``scripts/check_import_boundary.py`` — never engine internals
(``brainpalace_server.storage.*`` and other ``services.*``). See
``docs/superpowers/specs/2026-07-06-phase8-life-scaffold-import-gate-SPEC.md``.
"""

__version__ = "26.7.3"
